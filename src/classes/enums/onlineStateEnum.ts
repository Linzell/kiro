/**
 * Get the online state from the online state code
 */
// eslint-disable-next-line no-shadow
enum OnlineStateEnum {
  ONLINE = 'online',
  OFFLINE = 'offline',
  AWAY = 'away',
}

export default OnlineStateEnum;
