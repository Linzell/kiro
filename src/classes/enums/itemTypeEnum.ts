/**
 * Get the item type from the item type code
 */
// eslint-disable-next-line no-shadow
enum ItemTypeEnum {
  FOLDER = 'Folder',
  PAGE = 'Page',
  TAG = 'Tag',
  USER = 'User',
  TEAM = 'Team',
}

export default ItemTypeEnum;
